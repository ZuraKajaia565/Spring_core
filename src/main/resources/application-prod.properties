spring.application.name=gymCRM
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
spring.jpa.hibernate.ddl-auto=update
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.url=jdbc:mariadb://gymcrm-rds.cw168m0ycth1.us-east-1.rds.amazonaws.com:3306/gymcrm?useSSL=true&serverSslCert=/opt/mysql/server-ca.pem&trustServerCertificate=true&requireSSL=true
spring.datasource.username=admin
spring.datasource.password=Zaza12&31234
spring.jpa.defer-datasource-initialization=false
spring.sql.init.mode=never
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Disable ActiveMQ completely
spring.activemq.enabled=false
spring.jms.enabled=false

# Connection pool configuration for RDS
spring.datasource.hikari.leak-detection-threshold=60000

# AWS IAM Database Authentication (Optional)
# Uncomment the following lines to enable IAM authentication:
# spring.datasource.url=jdbc:mariadb://gymcrm-rds.cw168m0ycth1.us-east-1.rds.amazonaws.com:3306/gymcrm?useSSL=true&enabledTLSProtocols=TLSv1.2&serverSslCert=/opt/mysql/server-ca.pem&useAWSIAM=true&generateSimpleParameterMetadata=true
# spring.datasource.username=${IAM_DB_USER:gymcrm-app-user}
# aws.region=${AWS_REGION:us-east-1}
# aws.rds.hostname=gymcrm-rds.cw168m0ycth1.us-east-1.rds.amazonaws.com
# aws.rds.port=3306

management.endpoints.web.exposure.include=health,metrics,info,prometheus
management.endpoint.health.show-details=always
management.prometheus.metrics.export.enabled=true

# Health check configuration for RDS
management.health.db.enabled=true

# Logging configuration for database connectivity
logging.level.com.zaxxer.hikari=DEBUG
logging.level.org.springframework.jdbc=DEBUG